[

 %% Start up gproc in global mode
 {gproc, [
          {gproc_dist, all}
         ]},

  %% Sync settings (dev only)
  {sync, [
          {growl, all},
          {log, all},
          {non_descendants, fix},
          {executable, auto},
          {excluded_modules, []}
         ]}

].

