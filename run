#!/bin/sh
apps="sync,ranch,crypto,cowlib,cowboy,uuid,jiffy,gen_leader,gproc,pixel"

init_code="[io:format(\"~p~n\", [application:start(A)]) || A <- [$apps]]."

erl -sname pixel -pa $PWD/deps/*/ebin \
    -pa $PWD/ebin -config sync -eval "$init_code" \
    -gproc gproc_dist all

